terraform {
  # Фіксуємо мінімальну версію Terraform
  required_version = ">= 1.0"

  # Тут також зазвичай налаштовують "backend" (збереження стану в хмарі),
  # але для завдання використовуємо локальний стан (за замовчуванням).
}